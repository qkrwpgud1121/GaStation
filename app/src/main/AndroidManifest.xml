<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.main">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Main"

        android:usesCleartextTraffic="true"

        tools:ignore="ExtraText">
        <activity android:name=".AreaList" />
        <activity android:name=".MapView" />
        <activity android:name=".SearchStation" />
        <activity android:name=".FAV" />
        <activity android:name=".AREA" />
        <activity android:name=".FAR" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAlpMeVRijL7XH5MHJvKII4qbS-EnLwQfE" />

        <activity android:name=".AreaDetailActivity">
        </activity>
    </application>

</manifest>